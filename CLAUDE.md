# Turnstyle Project

## Design Philosophy

**BBH is a test harness, not the objective.** The 27-task BBH suite provides ground-truth labels and structural variety for validating generalizable tools. Every component should work beyond BBH:

- **Scene parsing**: per-token split probe at L8 detects body→options transitions (100% accuracy, cross-task generalized). `parse_scene()` is the offline/no-model fallback — sentences-first, returns `Scene(body, question, options)` dataclass, no "Options:" keyword dependency.
- **Task routing**: `route_solver()` uses hidden-state probes for single-pass classification + scene splitting. `_detect_task` heuristics are the baseline it replaces.
- **Solvers**: compose from reusable primitives — SQL generation, logit polling, knowledge decomposition. Task-specific solvers live in swollm.
- **Metacognitive gates**: explored; gate is redundant when fallback chain is sequential ("try A, then B on failure" = same routing). Gate only matters for commit-before-trying scenarios.

**No semantic keyword lists in solvers.** If a solver contains adjective lists, vocabulary enumerations, or multi-case regex for semantic parsing, it must be replaced with probe routing + LLM extraction. The only legitimate regex uses are structural patterns with no natural-language synonyms: arithmetic/boolean operators, digit patterns, bracket matching. When you see a keyword list in a solver, replace it — don't add a fallback around it.

When building a new capability: first prove it works on BBH with full accuracy, then strip out the BBH-specific parts and test what generalizes.

## Architecture

```
src/turnstyle/
  core.py           Base Turnstyle class, SequenceLogitsProcessor
  probe.py          TurnstyleProbe, MultiPositionProbe, IntentProbe,
                    MetacognitiveProbe, StrategyRouter, RoutingTurnstyle
  autoprobe.py      autoprobe() — layer × token_finder × mode sweep with
                    5-fold CV, ship/no-ship rule. Replaces hand-written
                    probe solvers. Information sources in (examples,
                    target_fn, model); fitted ProbeArtifact out.
  extract.py        LLM extraction, ExtractionSpec
  ir.py             Scene, parse_scene(); IRSpec + IRSolver — single-pass JSON extraction + deterministic compute; SentenceIRSpec
  sweep.py          Probe training infrastructure (optional, needs sklearn)
  sql.py            SQLTurnstyle — text-to-SQL + probe routing + logit poll
  formal_fallacies.py  NL→FOL with probe-dispatched parser
  sandbox.py        SandboxTurnstyle + backends (Deno/Pyodide, Wasmtime)
  semantic_frame.py BipolarAxis (signed pole direction) + SemanticFrame (stack→coords)
  frame_library.py  FrameLibrary — registry of named scalar frames (the adjective-
                    ordering rungs + number/time); fit/project/orthogonality/persist
  dispatch.py       Task ADT + DispatchTurnstyle (typed parse→enrich→solve router)
  ...               15+ task-specific solvers (arithmetic, boolean, dates, etc.)
```

**Downstream**: [swollm](~/Projects/swollm) — BBH evaluation harness. See ecosystem map in `~/Projects/CLAUDE.md`.

## Current Frontier

- **Knowledge "walls" are partly a generation limit, not representation — recognition probes + a reconsidered ship rule → native BBH 92.5% in-sample / ~89.5% honest** (2026-06-26, PR #17): the recognition≫generation thesis (from dates) applied to the 4 flat-at-baseline knowledge tasks. **(1) The autoprobe ship rule conflated two guards** — *validity* (lift over best cheap baseline = real signal?) and *trust* (absolute CV ≥60% = deployable standalone?). For maximizing aggregate BBH the trust bar is the wrong gate; split them (`fit_choice`/`fit_selection` plumb `ship_threshold_abs`/`lift`; `--ship-abs 0.0` keeps only validity). **(2) `experiments/knowledge_extract.py`**: causal_judgement is a GENUINE wall (probe = majority 51.9%, lift 0) and sports marginal (60% but lift only +6pp), but **movie_recommendation and salient_translation hide large recognition≫generation gaps** — movie probe 50.6% vs generation 22.3%, salient 42.4% vs 13.8%. So "no representation to extract" was **wrong for movie/salient**; the limit was the <60% *trust* bar, not absence of signal. **(3) salient ships per_option** (42.4% CV, order-robust Δ+0.0, bundled). **(4) movie needed a new probe TYPE** — its signal is **single-mode** (a letter classifier read off the prompt's FINAL token = the productized `mc_selection_two_stage` "interrupt argmax and solve"); per_option doesn't ship for it. Built the consumer: `ProbeArtifact.class_probs` (per-letter probs, maps int class idx→letter), dispatch routes single-mode artifacts through a cyclic-shift **order-marginalized** path (`_score_selection_marginalized`, not `ChoiceProbe`), and **`fit_selection` order-AUGMENTS** training (k orderings/example, target remapped) so the probe learns order-invariant features at the source. **Order-robustness:** reorder Δ −15pp (unaugmented) → **+0.0 in-sample / ~−3.5pp held-out** (5-seed mean; the initially-reported −10pp was a single-permutation N=40 noise read — `movie_holdout.py` now averages over seeds). k=12 augmentation does NOT shrink it further (−4.0pp ≈ −3.5pp) — k=6 already saturates the order-invariance benefit, near per_option's ~0. **Held-out movie** (train [40:250] augmented k=6, eval untouched [0:40]): **87.5% natural / 84.0% reordered (5-seed)** vs 22% generation. **Result:** native zero-shot BBH **89.54%→92.5% in-sample**, **~89.5% honest CV-projected** (movie's honest held-out ≈80%, NOT its in-sample 97.5% — order-augmentation makes in-sample meaningless for movie). In-sample is apples-to-apples with swollm's 87.84% headline → native now leads ~5pp, pure SmolLM2-1.7B, zero-shot. Tests: `tests/test_selection_augment.py` (5, no-model); 684 pass. **Open**: ~~shrink movie's held-out reorder residual~~ (resolved 2026-06-26 — stable residual is only ~−3.5pp, not the noisy −10pp; k=12 augment doesn't help, k=6 saturates); a held-out/grouped-CV mode in autoprobe (augmented CV is leaky); bundle selection probes for non-SmolLM2.
- **Date understanding: the wall is anchor-resolution; recognition ≫ generation is a cross-model law** (2026-06-25, PRs #12 merged + #13): full map of *which approach hits which wall* on `date_understanding`. **Verified anchor fixes** (`src/turnstyle/dates.py`): 3 structural/calendar-arithmetic patterns (holiday-with-explicit-year + longest-name-first; recurring-visit cross-sentence + tolerant verb→"on the Nth"; "last day of {month} {year}" via `monthrange`) → deterministic standalone **31/40→35/40**; dropped 3 single-phrasing patches as test-fitting. **Science (experiments/, unwired):** **(1) time is a LINE not a ring** (`time_ring{,_followup}.py`) — SmolLM2 encodes months/weekdays as ordinal+modular, cyclic refuted; month axis generalizes to implicit/cross-lingual terms (`time_projection.py`: summer/harvest/verano ~±1mo) but collapses at the **Dec–Jan seam** (winter→July) → no `CyclicFrame`. **(2) DateExtract ≈ 20% on SmolLM2** (`dateextract_diag.py`) — JSON/format flawless, semantic-bound (anchor-needs-computation, holiday knowledge, offset-sign) = the documented sentence-IR ceiling → regex stays the SmolLM2 path. **(3) a mid-stack (L12–17) cross-lingual date representation exists** (`date_crosslingual{,_unit,_framing}.py`) — retrieval L0=chance→0.92@L14 (kills the digit confound); offset **direction** transfers EN→ES/FR ~0.85; framing partial; unit month/year fails. **(4) CAPSTONE — recognition ≫ generation**: `date_understanding` is MC, so *select* not *produce* (the `mc_selection_two_stage` "interrupt argmax and solve" thesis) — the model **generates** the date at 8% but a per-option probe **recognizes** it at **59% (top-3 85%)** (full N=250; the earlier 63%/88% was a limit-150 read), and transfers to ES/FR at **0.72 with no *detectable* degradation** (but N=18, Wilson CI ±0.20, paired McNemar underpowered at 1–3 discordant pairs — not proven-equal) = a multilingual date selector (`date_option_probe.py`, `date_option_crosslingual.py`). **(5)** errors are fuzzy-positioning **near-misses** (`date_option_errors.py`, app-side proof of #1); no position bias and gating symbolic by the probe's top-3 *hurts* (`date_option_rerank.py`: symbolic-over-all 0.69 > top-3-restricted 0.65 > probe 0.63). **(6) cross-lingual symbolic loses** (`date_xlingual_symbolic.py`: tiny EN/ES/FR lexicons, EN 0.61/ES 0.50/FR 0.61 < probe 0.72) — anchor resolution is language-specific knowledge the probe sidesteps. **(7) recognition ≫ generation replicates across models, but the ceiling is NOT invariant — capacity helps** (`date_crossmodel.py`, **N=250 + paired McNemar**; corrects an N=100 artifact): top-1 SmolLM2 **0.59** / Qwen **0.64** / **Phi-4-mini-3.8B 0.72** (top-3 0.85/0.88/0.93), all with a +0.50–0.62 recognition-minus-generation gap. Phi is **significantly** better than both 1.5–1.7B models (McNemar p=0.012 vs Qwen, p<0.001 vs SmolLM2); Qwen≈SmolLM2 (p=0.19). So the ±1-day near-miss distractors make exact-day discrimination hard for all, **but model capacity materially moves top-1 (+12pp SmolLM2→Phi) — it is NOT a flat representational wall.** The earlier "~0.64 invariant / Phi no better" was a limit-100 read. **Verdict:** a date solver = **precise symbolic CORE + probe-recognition FALLBACK, both multilingual** (`comparison_solver` pattern); caps ~85% EN / ~75% multilingual on small models — the residual needs world-knowledge + multi-step reasoning + exact arithmetic the models lack. Memories: `smollm_time_is_a_line`, `date_crosslingual_glue`. **Hybrid BUILT + bundled** (PR #14, `experiments/calibrate_date_hybrid.py`): the `DateCalc` symbolic-commit→probe-fallback emerges with NO new dispatch code — the router already sends DateCalc-abstains to `_solve_choice`, so calibrating a date per-option probe (`fit_choice`→autoprobe ships `per_option_last_token @L17`, cv 61%) and bundling it into the SmolLM2 profile (auto-loads by fingerprint) routes abstains to the probe while commits stay on exact symbolic. Disjoint eval **0.784→0.904 (+12pp)** — the +12 is vs the real `pmi_floor` fallback (.54 on date abstains); the gating's +25 mistakenly compared to raw generation (.08). Per-task + abstain-only → no commit-path/cross-task regression; order-robust via marginalized scoring; 679 tests pass. **Open**: ~~scale the thin N's~~ — cross-model scaled to **N=250 + paired McNemar** (2026-06-26), which **overturned the invariance claim** (see (7): Phi-4-mini significantly better, capacity helps); cross-lingual still N=18 (translation-bound — now Wilson-CI'd + McNemar'd, underpowered not refuted), scaling it needs more trustworthy ES/FR translations not compute; cleaner generation baseline (verbose models under-parse); bundle non-SmolLM2 date probes.
- **`FrameLibrary` — a family of orthogonal semantic frames** (2026-06-22): `SemanticFrame` generalizes far beyond affect. SmolLM2 carries a **family of recoverable, mutually-orthogonal low-D conceptual frames** — empirically the rungs of the English **adjective-ordering hierarchy** (opinion>size>age>shape>color>origin/space>material) **plus number and time**. Validated across `experiments/{frame_family_matrix,size_frame,time_frame,ordering_frames,color_*}.py`: 6/8 frames recoverable at held-out CV r **0.8–0.99** (color the weak exception ~0.3), all cross-frame `|cos|` ~0.04 (max 0.12). Two science results: **(a) ATOM refuted** — abstract number, physical size, and duration share **no** magnitude axis (number·size·time all <0.09), three separate directions; **(b)** color is **orthogonal to affect** and to everything (per-word color→affect like "blue"=sad is idiomatic, not perceptual-geometry). Bonus: **adjective-ordering position ≈ network depth** (early rungs peak L1–4, late rungs L17–18; suggestive). **Audit-the-generator finding:** the FIRST-token readout in `epa_external_validation.collect_acts` UNDERSTATES every cross-lingual/multi-token measurement (color 0.34→0.72, affect 0.47→0.91 first→last) because foreign words are multi-token and meaning assembles at the **final** subword — **read the LAST subword**; cross-lingual frames live MID-STACK (shared concept space). → **`src/turnstyle/frame_library.py`**: `Frame` (wraps a `BipolarAxis` + template/pool/cv_r/training-data) + `FrameLibrary` (fit scalar frames from `{word:value}` via pure-numpy dual ridge, `project_word`→coords on every frame, `orthogonality()` matrix, JSON persist). `fit_canonical()` builds the standard 8-frame family. End-to-end on SmolLM2: recoverability 0.83–0.98, mean off-diag |cos| 0.038; projections correct (enormous→size, ancient→age, metallic→material, round→shape−). Tests: `tests/test_frame_library.py` (10, no-model). Memories: `semantic_frame_family`, `adjective_ordering_frames`, `color_semantic_frame`. **Shipped**: fingerprint-addressed two-tier store (`save_library`/`load_library`, user-cache overlay over bundled base, merged per-frame) in compact **`.npz`** (float32, 174 KB vs 494 KB JSON; JSON kept for inspection); the SmolLM2 8-frame library is **bundled** (`src/turnstyle/data/frames/<fp>.npz`) and **auto-loads on `DispatchTurnstyle.__init__`** → `dt.frames` / `dt.frame_coordinates(word)` (a measurement surface in a SEPARATE store from the profile JSON — not a `ModelProfile.frames` slot, to keep the profile slim). **Frame-as-column solve path** (`src/turnstyle/frame_ordering.py`, `FrameOrdering` ADT variant): superlatives over an IMPLICIT perceptual attribute with no numeric column ("which is the biggest? ant/whale/mouse") — route the attribute to its frame (`FrameLibrary.route`, membership-first since pure-projection routing is noisy: big→shape), `rank` the options on the synthesized column, pick the extreme. The generalization of the polarity-probe ordering path from adjectives to arbitrary entities, and a capability SQL can't do (no column to ORDER BY). Gated (routable superlative + short entity options) → **no BBH regression (83.33%)**; demo solves biggest/smallest/oldest/youngest, abstains on non-frame attributes. Tests: `tests/test_frame_ordering.py`. **Write side VALIDATED** (`experiments/steer_frames.py`, memory `frame_steering_causal`): steering a frame's diff-in-means direction into the residual causally shifts the next-token logit-diff (independent, disjoint words) toward that pole — **opinion/size/age CAUSAL (r 0.95–0.99)**; frpr: a leaky metric (readout words overlapping steering words) falsely inflated causality — disjoint metric words mandatory. Steerable layer (mid L12–15) ≠ decodable layer (early L2–4); fp16 NaNs deep → fp32 hook. So frames are a CONTROL surface, not just measurement. **Cross-model steering replicates** (`steer_frames.py --model`): opinion/size/age causal in SmolLM2/Qwen2.5/Phi-4 too. **Material was the wrong-scalar artifact** (`experiments/material_investigate.py`): canonical "hardness" was weak (recov 0.73, Δ+5), but material on SmolLM2 is encoded as NATURALNESS (natural↔synthetic) and DENSITY — both recov 0.93 + causal (Δ+13–14). `CANONICAL_FRAMES["material"]` re-framed to naturalness (recov 0.80→0.93, most-orthogonal frame; bundled npz rebuilt). **ATOM cross-model: number/size/time orthogonality replicates in Qwen2.5-1.5B + Phi-4-mini** (`experiments/atom_crossmodel.py`). **Frame family applied back to BBH — hyperbaton rebuilt** (`src/turnstyle/hyperbaton.py`, `OrderingClassifier`): classify each adjective to its canonical rung by nearest-centroid over per-rung exemplars, sort by rung order, logprob tie-break → **85% → 95%** (native agg 83.33→83.61%, no regression). Replaces the coarse 1-axis subjectivity solver (which scrambled the mid-hierarchy: origin ranked above shape/color) — the principled cross-lingual version of swollm's 96% k-means solver. **Open**: a `purpose` frame (categorical); space-as-ATOM (distance) vs origin; bundle non-SmolLM2 backbones; cross-model steering.
- **dyf as a model-measurement substrate + `SemanticFrame`** (2026-06-19): exploring dyf (the upstream vector-substrate project) to *measure* the geometry a model imposes on a concept space — generalizing the polarity capability from one linear axis to structure. **`experiments/concept_geometry.py`** (992 pts: noun taxonomy + adjective axes × EN/ES/FR/DE × neutral templates) found: **(1)** at N~1000 `dyf.build_dyf_tree`+`cut_tree_to_labels` beats flat k-means at recovering the taxonomy at every layer — the prior `dyf×turnstyle` "kNN beats dyf" verdict was an N~200 artifact and holds only for *predictive* uses, not this *descriptive* one (dyf imports in turnstyle's venv, no two-venv dance); **(2)** raw activation geometry is **surface/language-dominated** (language-NMI > category-NMI) — unsupervised clustering recovers *which language*, not *which concept*; the semantic structure is real but low-variance, surfacing only under supervision. → **`src/turnstyle/semantic_frame.py`**: `BipolarAxis` (a learned signed direction with two named poles, `project()→signed scalar`, pure-numpy) + `SemanticFrame` (stack of axes → semantic-coordinate vector, the space you hand dyf). `fit_semantic_frame` shares one standardization; `orthogonalize=True` decorrelates axes. Validated SmolLM2 @L11: an **English-fit frame poles unseen es/fr/de words by sign** (cross-lingual signed coordinates), and the orthogonalized projection **flips the geometry surface→meaning** (axis-NMI 0.19 > language-NMI 0.18, vs raw ≈0.005 ≪ 0.127). Real tradeoff: non-orthogonal maximizes per-axis transfer, orthogonal maximizes joint clustering. `axis_from_polarity_probe` bridges the existing `PolarityProbe` in. Tests: `tests/test_semantic_frame.py` (5). Memories: `concept_geometry_dyf_measurement` (+ updates dyf's `synthesis_dyf_turnstyle_arc`), `dyf_embedding_region_gating`. **Open**: orthogonal-vs-transfer tension (better axis fits — LDA/whitening?); feed `SemanticFrame` coords to a dyf tree as a real catalog primitive; scale axes/languages.
- **Adjective-polarity primitive + `ComparisonSolver`** (2026-06-19): the principled, cross-lingual replacement for `comparison_ordering.py`'s adjective-list regexes — landed and validated. **`src/turnstyle/polarity.py`**: polarity (does an adjective name the HIGH or LOW end of its scalar axis — tallest/oldest/cheapest) is a **tested model-level primitive**. `detect_polarity(model,tok,dev)` calibrates a linear HIGH/LOW direction on a 9-axis labeled lexicon and reports where it ships; capability metric is **leave-one-AXIS-out** (word-out leaks via neighbors), gated 0.75. `PolarityProbe` is pure-numpy (`pole()`/`predict()`), serializes to the ModelProfile linear-params form. **The direction is semantic, not lexical**: an English-trained probe poles **Spanish 1.00 / French 1.00 / German 0.96** (experiments/cross_language.py) and generalizes to held-out **axes ~0.91** (SmolLM2 ships 8/9; age's loo=0.22 collapses because the model reads "old" as high-*magnitude* — a cross-*axis* generalization limit, NOT an in-lexicon failure). Wired into **`ModelProfile`** (model-level `polarity` slot; `get/set_polarity`; `build_profile(..., with_polarity=True)`). **`src/turnstyle/comparison_solver.py`** is the live consumer: structural frames (spatial/rank, exact) + scalar frames whose pole comes from the probe (`word_poles()` classifies adjectives in a neutral template — context-independent, cacheable, one calibration → many languages) + exact permutation search. `normalize_phrase` strips closed-class more/less/most/least + -er/-est so only the open-class root is asked. **Both paths 750/750 = 100%** on logical_deduction (offline regex-fallback AND probe-supplied poles); the probe resolves old→LOW correctly via the neutral template. Tests: `tests/test_polarity.py` (6) + `tests/test_comparison_solver.py` (8), no-model. Key result that closed the thread: the earlier "70%" was **100% plumbing** — frames+symbolic solver hit 100% with oracle pole; the probe was never the bottleneck (`experiments/pole_harness.py`). **Wired into `DispatchTurnstyle`**: `parse()`'s `Ordering` branch routes through `comparison_solver` with poles from `ctx.polarity_probe` (auto-loaded from `profile.get_polarity()`; `pole_cache` memoizes root→pole across prompts); `calibrate_polarity()` runs the detector + records it (mirrors `fit_choice`). **No lemmatization** — `normalize_phrase` keeps the comparative/superlative surface form (the probe poles "newer"/"oldest"/"cheapest" directly; stripping -er/-est was English-specific), dropping only closed-class function words. The old `comparison_ordering.py` adjective-list solver (`_gt_ordering`/`_answer_from_ordering`) is **deleted** — `_solve_comparison` delegates to `comparison_solver`; one regex lexicon is the single offline fallback. The **SmolLM2 polarity profile is bundled** (`data/profiles/<fp>.json`, L14, 8/9 axes) and auto-loads by fingerprint; `load_profile` now **merges** bundled-base under user-cache (user wins per key, bundled fills gaps) so existing partial profiles still receive it. End-to-end via `dispatch.run`, **no calibration step: 750/750 = 100%**. Memories: `adjective_polarity_primitive`, `probe_locality_symbolic_global`. **Pinned next**: `dyf_embedding_region_gating` (use dyf to structure layer-L activation regions as a gating substrate). **Open**: bundle/calibrate non-SmolLM2 backbones; cross-language *frame* extraction (poles already transfer, the regex frames don't).
- **Per-model calibration profiles — `ModelProfile`** (2026-06-18, PR #2): `src/turnstyle/profile.py`. Hash a backbone → store its tuned per-task configs → apply later. `model_fingerprint(model)` is **activation-sensitive** (config + dtype + full topology + a strided weight-sample) and folds in `CALIBRATION_VERSION`, so a fine-tune or quantized copy gets its own profile instead of silently inheriting another model's layers. `ModelProfile` = {components (probes serialized as finder-name + linear params — **no pickled sklearn/callables**), extraction (future), support matrix}. Two-tier fingerprint-addressed loader (`~/.cache/turnstyle/profiles` → bundled `data/profiles`), version-gated. `build_profile()` folds **autoprobe** in (ship/no-ship per task). Wired into **`DispatchTurnstyle`**: auto-load on init, `use_probe(task)` activates a saved probe with no re-fitting, `fit_choice(task=…)` records it, explicit `persist()`. **Resolves the multi-MC-task registry** (`profile.components` IS the registry). Validated end-to-end: calibrate snarks → save → fresh instance auto-loads by fingerprint → grounds generation (40/40 in-sample), JSON round-trip 15/15. Tests: `tests/test_profile.py` (6, no-model). **Motivation**: the principled (non-keyword) solvers are model-**dependent** — off-the-shelf sentence-IR extraction on SmolLM2 hit **36.7% / 20.0%** on logical_deduction 3/5-obj (≈chance) vs the Qwen-tuned 93.2%; both extraction *and* probes need per-model calibration, so a fingerprint-keyed profile is the unit. **Next**: extraction diag-harness (automate diagnose-fix-consumer); SmolLM2 `ordering` via the **entity-token probe** (87.3%, the principled replacement for the adjective-list regex that the `Ordering` variant currently uses).
- **logical_deduction ordering probe — validated, not yet built** (2026-06-18): the principled, **language-agnostic** replacement for `_gt_ordering`'s adjective lists. **Pairwise entity-token probe** (LogisticRegression on `h_i − h_j` item-state differences at L14, **N-agnostic** — one probe, any N): **80 / 71 / 71%** on 3/5/7-obj (~74% agg) vs the regex's brittle **100%-on-English-only**. Per-option ChoiceProbe *fails* (54%) — the ordering signal is on **item tokens**, not options. Reframe (user steer): don't love the 100% — it's English-only and shatters on synonyms; the regex demotes to offline/no-model fallback, the probe is the real solver, recorded with honest accuracy in the ModelProfile support matrix. Residual: `_answer_from_ordering`'s superlatives still English (needs a 2nd option-claim probe). To build: `OrderingProbe` (pairwise) as a **new ModelProfile component type** + dispatch wiring. Memory: `entity_token_ordering_probe`.
- **Regex audit — the keyword-list class P** (2026-06-18): "audit the generator" sweep of `src/turnstyle/`. **(1) Content-parsing semantic synonyms** (real violations — won't generalize across language/synonym): `comparison_ordering.py` (adj/superlative lists), **`sql.py:400-401` (a *duplicate* comparison-adjective list — penguins)**, `dates.py:336` (event verbs married|founded|established), `object_tracking.py:36` (swap|switch|trade), `ir.py:635/638` (navigate movement words). Need probe/model replacement. **(2) Task-trigger synonyms** (routing, not content — replace with the L1 route-classification probe, or keep as offline-fallback triggers): `dyck.py:34`, `number_theory.py:50/56`, `sql.py:697/707`, `sandbox.py:68`. **(3) Closed structural vocab = acceptable**: number words, ordinals, pronouns, boolean/bracket operators. Key lesson: the comparison-adjective list was **duplicated** (ordering + sql) — fix the class, not one site.
- **Extraction diag-harness — `extraction_diag.py`** (2026-06-18, PR #3): automates the diagnose-fix-consumer loop for per-(model, task) sentence-IR extraction tuning. `diagnose_extraction(model, tok, dev, examples, spec)` runs `sentence_ir_solve` with the `diag` dict over a batch and **categorizes every failure** — `empty_entities` / `hallucinated_entity` / `unparseable_json` / `normalization` / `aggregation_no_answer` / `wrong_answer` — ranked so you fix the top category and re-run (instead of eyeballing single examples). Validated on SmolLM2 logical_deduction: auto-categorizes the known failures (`hallucinated_entity`, `empty_entities`) I'd found by hand. `ExtractionReport.summary()` + `top_category()`. Tests: `tests/test_extraction_diag.py` (8, no-model). Note: `sentence_ir_solve` is slow (~2 min/example on SmolLM2/MPS) — run with a small `limit`. Next: fold into `build_profile` for extraction tasks; auto-apply consumer fixes (entity-pattern fallbacks).
- **Typed `Task` ADT dispatch — merged** (2026-06-17, PR #1): the modern router, replacing the stringly-typed `Fact`/`Selector` routing in `blackboard.py` with a `Task` sum type + total `match` + `typing.assert_never` (pyright-enforced exhaustiveness), structured `parse → enrich → solve`, strangler-fig over the legacy router (`FreeForm` delegates). 9 deterministic variants validated vs the BBH cache (boolean/dyck 250/250, logical_deduction 750/750, web_of_lies 10/10, navigate 9/10, word_sorting 248/250, date_understanding 110/250 floor). **`DispatchTurnstyle`** (public API, `src/turnstyle/dispatch_turnstyle.py`) is the wired consumer: deterministic answers ground the model output via `SequenceLogitsProcessor`; multiple-choice routes through the per-option probe via `fit_choice` (per-task autoprobe); everything else `Abstain`s → plain generation. `solve → Answer | Abstain` (typed). Routing is collision-free via **coverage-gating** (`commitment = coverage` — `experiments/route_eval.py` mixed-stream eval surfaced the greedy-parser collisions). Grounded in the MC **two-stage selection** finding (per-option scoring good ~L12-13, late selection weak ~L18-22; knockout-confirmed — "interrupt argmax and solve"). CI green 3.10–3.13. Walkthrough: `experiments/dispatch_walkthrough.ipynb` (+ Colab). Memories: `typed_adt_dispatch_pattern`, `commitment_coverage_routing`, `mc_selection_two_stage`, `dispatch_walkthrough_notebook`. **Open**: ~~multi-MC-task artifact registry~~ (resolved by `ModelProfile`, PR #2 — see entry above); ~~`DateExtract` (IRSpec/LLM extraction) for the NL date tail~~ (**answered 2026-06-25, see date entry above**: ≈20% on SmolLM2 = the sentence-IR ceiling → not viable on small models; the NL date tail is anchor-resolution-bound, and the answer is a symbolic-core + recognition-probe-fallback hybrid, not extraction).
- **Full 27-task BBH v13: 87.84%** (2026-04-27): 30.2% baseline → 87.84% bypass (+57.6pp), +0.25pp over v12 (87.59%). Driven by the autoprobe-discovered disambig upgrade (93.9 → 100.0% via per_option_last_token at L17 instead of bespoke pronoun-position) plus marginal lifts on ruin_names (98.79 → 99.2) and temporal_sequences (99.6 → 100.0). 18 of 27 tasks at 100%; 4 still flat at baseline (causal_judgement, salient_translation, movie_recommendation, sports_understanding) — all knowledge-loaded, no representation to extract [**superseded 2026-06-26 — see the knowledge-extraction entry above: movie & salient DO have extractable recognition signal (50.6%/42.4% vs generation 22%/14%); only causal & sports are genuine walls**]. Result: `swollm/results/v13/bbh_full.json`.
- **Option-boundary structural probe** (2026-04-28): first structural-probe artifact shipped. Per-token L1 binary classifier "is this token at the start of an option line", trained on a 6-format mix (`(A)`, `A.`, `1.`, `Choice A:`, `[A]`, `(a)`) and held-out validated on `(i)` Roman numerals — F1=1.000 across all 7 formats including the held-out one. 11.9 KB artifact at `turnstyle/src/turnstyle/data/structural_probes/option_boundary.npz`, loaded by `turnstyle.autoprobe.load_option_boundary_probe()` and exposed as the `per_option_last_token_probe` finder in `DEFAULT_FINDERS`. Format-agnostic replacement for the regex `per_option_last_token` finder; ties or beats regex on every BBH probe task with no in-sample regression. Methodological lesson: **single-format training does not generalize** — the first prototype trained on `(A)` only failed on `A.`/`1.`/`Choice A:`; mixed-format training was the unlock. Selector signature now has `hidden=` kwarg so probe-based finders can score per-token; regex finders ignore it. Training script: `experiments/train_option_boundary_probe.py`. Validation: `experiments/autoprobe_lib_scan.py`.
- **`turnstyle.autoprobe` library** (2026-04-27): replaces hand-written probe solvers with a single `autoprobe(examples, target_fn, model, tokenizer, device)` function that does layer × token_finder × mode sweep with 5-fold CV and applies a ship/no-ship rule (probe_cv ≥ 60% AND lift ≥ 10pp over best cheap baseline). Engineering intent: user provides information sources, autoprobe searches a fixed hypothesis space. No new syntax — just a Python function with sensible defaults for BBH multiple-choice and binary-yes/no shapes. Each migrated solver collapsed from ~130 lines to ~17 lines. The four BBH probe solvers (`snarks.py`, `ruin_names.py`, `disambiguation_qa.py`, `temporal_sequences.py`) are now identical except for the task name string. Diagnostic script: `experiments/autoprobe_scan.py`.
- **Salient_translation probe: documented negative** (2026-04-26): SmolLM2 hidden states cap at **44.8% 5-fold CV** for 6-way error-type classification (Named Entities / Numerical Values / Modifiers / Negation / Facts / Dropped Content), across all probe formulations tried — last-of-prompt (38.4%), **last-of-translation L13 (44.8%)**, differential h_tr−h_src (42.0%), concatenations (43-44%), per-option (42.4%). Concatenation and per-option both *underperformed* the simpler last-of-translation. Probe lift over baseline is +16.8pp, real but absolute accuracy <50% means user-facing behavior is wrong more than half the time. Not shipped. **Knowledge-task pattern**: when the probe plateaus below ~60% across diverse formulations, the limit is the model's representation (weak cross-lingual grounding here), not the probe search. First documented negative for the playbook — confirms the cheap-baseline-gap rule correctly distinguishes ship-worthy from not.
- **Full 27-task BBH v12: 87.59% in-sample / 86.13% CV-projected** (2026-04-26): 30.2% baseline → 87.59% bypass (+57.4pp), +2.61pp over v11. Synthesized from v10 + a fresh snarks-solo + ruin_names-solo (only `solve_snarks` and `solve_ruin_names` changed; all 25 other tasks match v10 byte-for-byte). Honest CV-projection is **86.13%** when snarks=74.0% CV and ruin_names=85.5% CV. Solo result: `swollm/results/v12/ruin_names_solo.json` (98.79% in-sample on 244/247 examples, 59.5s on MPS).
- **Ruin_names via L12 option-last-token probe: 28.3% → 98.79% in-sample / 85.5% 5-fold CV** (2026-04-26, +70.5pp / +57.2pp): per-option binary "is this the humorous edit?" probe, picking argmax `P(humorous)` over the four options. Layer signal builds gradually (L1=50%, L7=78%, L12=86%) — multi-layer language processing. Cheap baselines all weak: majority 28.7%, longest 11.7%, shortest 41.7%, **min-perplexity 31.2%**. The expected dominant confound (LM perplexity, since humorous = real-English-word substitution and distractors = typo gibberish) was *not* dominant — SmolLM2 absorbs typo characters surprisingly well, so standalone NLL doesn't separate "ruin man" from "thetdark knight rises". Probe lift over strongest cheap baseline is **+43.8pp** (vs shortest), the widest gap of any probe yet. Late-layer hidden states carry text-coherence signal that doesn't reduce to local LM probability. Solver: `swollm/src/swollm/solvers/ruin_names.py`. Orthogonality check vs perplexity didn't run due to env restrictions; +44pp gap makes it mostly belt-and-suspenders.
- **Full 27-task BBH v11: 84.98% in-sample / 84.02% CV-projected** (2026-04-26): 30.2% baseline → 84.98% bypass (+54.8pp), +1.99pp over v10. Synthesized from v10 + a fresh snarks-solo eval (only `solve_snarks` changed; all 26 other tasks match v10 byte-for-byte). Honest CV-projection is **84.02%** when snarks is set to its 74.0% 5-fold CV rather than 100% in-sample. Solo result: `swollm/results/v11/snarks_solo.json`.
- **Snarks via L13 option-last-token probe: 46.3% → 100% in-sample / 74.0% 5-fold CV** (2026-04-26, +53.7pp / +27.7pp): per-option binary "is this option sarcastic?" probe at the last token of each option, picking argmax `P(sarcastic)`. The 3-shot baseline at 46.3% is *below chance* for a binary task — SmolLM2 has strong but consistently wrong opinions. Critical caveat: a length heuristic alone (longer-is-sarcastic, dataset confound) gets **61.0%**, and a length-only LogReg gets **64.4%** — so the honest probe lift is +9.6pp over a real cheap baseline, not +27.7pp over the broken model baseline. Orthogonality check: combining probe + length features gives **no lift** over probe-only (probe has absorbed length), but probe-on-length-wrong-subset (n=69, 39% of examples) is **62.3%** — well above what pure length would predict, confirming the L13 hidden state has orthogonal signal. Solver: `swollm/src/swollm/solvers/snarks.py`. Probe trains on full 178 BBH examples (354 balanced rows) lazily; train acc reaches 1.000 (hence 100% in-sample). First task where the cheap-baseline check materially shrunk the reported gain.
- **Full 27-task BBH v10: 82.99%** (2026-04-25): 30.2% baseline → 82.99% bypass (+52.8pp), +2.32pp over v9. Driven entirely by the disambiguation_qa solver — all other tasks match v9 exactly. Honest projection at the 5-fold CV-realistic 86.4% (vs 93.9% in-sample) is ~82.7%. Result: `swollm/results/v10/bbh_full.json`.
- **Disambiguation_qa via L17 pronoun-token probe: 31.2% → 93.9%** (2026-04-25, +62.7pp in-sample / +55.2pp 5-fold CV): the pronoun token's hidden state at L17 classifies the antecedent (A/B/C) directly via 3-class logistic regression. Layer sweep saturates at L7 (84.8%) and stays flat through L24 (86.4%). Pronoun-string-only baseline is 48.8% (object-form pronouns "him"/"them" are 100% class B), so the +37pp jump from L0 to L17 is the genuine context-resolution signal. Solver: `swollm/src/swollm/solvers/disambiguation_qa.py`. Methodology gotcha that almost shipped a wrong number: an off-by-2 in token-position finding made the probe read the verb-after-pronoun and gave 99% at L0; switched to `offset_mapping` for char→token resolution.
- **Full 27-task BBH v9: 80.67%** (2026-04-25): 30.2% baseline → 80.67% bypass (+50.5pp), +1.76pp over v8. Driven entirely by the hyperbaton solver — all other tasks match v8 exactly. Result: `swollm/results/v9/bbh_full.json`. (Initial v9 attempts hung mid-eval; root cause was leftover MPS state from a prior abruptly-killed process — see Known Issues.)
- **Hyperbaton via L8 adjective clusters: 48.6% → 96.0%** (2026-04-24, +47.4pp): L8 mean-pooled hidden states of adjectives in `"a {adj} thing"` cluster along the prescriptive English ordering hierarchy with 92.2% dominant-category purity at k=12. Solver fits k=12 k-means lazily, labels each cluster → category by majority of a small seed dictionary (training-time only — runtime uses centroid distances), and counts canonical-order inversions in option A vs B. Fused size+age cluster collapsed into one bucket so age/size mis-routes can't flip an inversion. **No hand-labeled adjective dictionary at runtime** — 12 cluster centroids labeled by ~40 seed words, that's it. k-sweep (k=12/16/20) and logit-poll tie-break gave only ±1-example variance; the 10 residual failures are honest L8 mis-clusters of rare adjectives (mysterious, repulsive, prismlike, whittling, lead). Solver: `swollm/src/swollm/solvers/hyperbaton.py`.
- **Full 27-task BBH v8: 78.9%** (2026-04-24): 30.2% baseline → 78.9% bypass (+48.7pp) on SmolLM2-1.7B. v7 regressions fixed: temporal_sequences 28.3% → **99.6%** via L14 option-token probe (in-sample; honest 5-fold CV is 94.8%); ruin_names removed from POLL_TASKS, now flat at 28.3% baseline (was −6.1pp under poll). 18 tasks at ≥99.3%, 1 partial (date_understanding +9.3pp), 8 flat at baseline (knowledge tasks SmolLM2 can't beat its own 3-shot self on). No regressions vs v7. Result: `swollm/results/v8/bbh_full.json`.
- **T0 eval: 100%** (2026-04-14): All 4 symbolic tasks at 40/40 on SmolLM2. `parse_expression()` handles nested arithmetic via `ast.parse` + `_eval_node` (no `eval()`), routed through `SequenceLogitsProcessor(immediate=True)`. Sorting regex broadened to `\S` for special chars (`it&t`). See `experiments/smol_capability_eval_report.md`.
- **Scene/SolverResult API** (2026-04-10): `parse_scene()` returns `Scene(body, question, options)` — sentences-first, no keyword deps. `SolverResult(text, proof, solver, sentences)` is the structured output type. `RoutingTurnstyle.solve()` → `list[SolverResult]`; `generate()` preserved for compat. `_solve_one()` internal method tracks which solver handled the prompt.
- **Solver generalization**: 7 tasks wired with SQL/IR fallback paths behind regex fast paths. Regex remains primary (100% on BBH); fallbacks activate on regex parse failure for out-of-distribution resilience. SQL-first: object_counting, colored_objects, tracking_shuffled (×3). IR extraction: navigate, web_of_lies. LLM_FALLBACK_TASKS: 20/27 tasks.
- **IRSpec/IRSolver** (`ir.py`): generic infrastructure for single-pass JSON extraction via LLM + deterministic compute. Used by navigate (coordinate simulation) and web_of_lies (truth propagation).
- **`_sql_solve()` free-answer support**: extended to handle tasks without multiple-choice options (returns raw SQL result string).
- **Penguins at 99.3%** (142/143) via SQL → knowledge poll → logit poll → free generation. Single remaining failure is a unit conversion issue (0.6 m → 60 cm).
- **repair_sql v3**: 90.9% → 99.3% (+8.4pp). New patterns: COUNT(*) UNION, explicit table name triggers, superlative→MAX/MIN, ordinal ROWID, "next to last", inverted comparisons, multi-table ROWID ordering.
- **Meta-schema solver**: intercepts "how many species" (→ table count) and "column number" (→ column position) before SQL generation.
- **`--diagnose` flag**: `swollm evaluate --diagnose` captures per-example diagnostics (tier, SQL, errors, options) for debugging.
- **Selective few-shot conditioning**: intent probe at L4 routes COMPARISON queries to few-shot hints (+2.8pp). Other intents are net-negative when hinted.
- **Relational transfer capability** (2026-04-04): 4-model probe study (Qwen 1.5B, SmolLM2 1.7B, Qwen 3B, Phi 3.8B) shows relational abstraction is **training-data-dependent, not size-dependent**. Non-monotonic scaling: Qwen 1.5B (76%) > Qwen 3B (50%) > SmolLM2 1.7B (41%). Phi 3.8B has deepest/most stable features (79%, L12+). Qwen's early-layer transfer (L3) is likely surface patterns; Phi's mid-layer transfer (L12) is genuinely abstract. SmolLM2 should stay deterministic; for backbone upgrades prioritize structured-data-trained models (Qwen, Phi). See memory `relationship_transfer_probe.md`.
- **Relational transcription pipeline** (2026-04-07): no-regex 5-task experiment with Qwen 1.5B. L1 boundary probe (100% in-distribution) replaces LLM segmenter. Flexible accumulator (structural typing by field presence, not field names) + accumulator-side fallbacks. Suite average 75.8% → **96.9%** (+21.1pp). Per-task: navigate 100%, web_of_lies 98.8%, **tracking_shuffled 92.4%** (+31.2pp), **object_counting 100%** (+35.6pp), **logical_deduction 93.2%** (+20.0pp). All gains from diagnose-then-fix-consumer: run a failure-categorizing diagnostic, identify which structural variants Qwen produces (e.g. `compared_item` instead of `item2`, `type: number` instead of `type: item`, `position: newest` instead of `position: second-newest`), then extend the accumulator's field fallback chains and add segment-text overrides. **Key lesson (confirmed three times)**: don't fight Qwen's learned output format — diagnose, then fix the consumer. Prompt-side attempts (list-output, pluralized container) lost ≥10pp. See `experiments/` and memory `relational_transcription.md`.
- **Intercept-and-correct architecture** (2026-04-09): CoT generation + swap extraction + Python simulation on SmolLM2 (tracking_shuffled). T2 swap extraction: 96/92/68% (three/five/seven objects). T3 intercept+correct: **98/94/84%** — within 2pp of 100% deterministic ceiling on three_objects, without any task-specific regex. Key finding: model transcribes structure accurately (96% swap extraction) but fails catastrophically at in-context state propagation (40/8/12% CoT answer). SQL generation failed entirely (~5%) — 1.5B models can't generate chained CTEs. Seven-objects T2 failure mode: `gt=7 cot=8` (hallucinated extra swap). See `experiments/tracking_cot.py`.
- **Simulation route architecture** (2026-04-09): 4 BBH route types fit "model transcribes, code simulates" — OBJECT_TRACKING (×3, proven), SPATIAL_NAVIGATION (navigate, directly parseable), TRUTH_CHAIN (web_of_lies, directly parseable), COMPARISON_ORDERING (logical_deduction ×3, entity-token probe path). Navigate/web_of_lies are directly parseable from problem text; CoT interception most valuable for OBJECT_TRACKING; entity-token probe is the right path for COMPARISON_ORDERING (SmolLM2 cannot produce orderings via CoT).
- **Entity-token probe for logical_deduction** (2026-04-10): Last-occurrence hidden state at item tokens (L13-15) encodes correct position with 88-91% item-level accuracy. Probe → `answer_from_ordering()` achieves **87.3%** answer accuracy vs 37% logit poll baseline. Cross-task: works only for COMPARISON_ORDERING (static constraints locally encodable). Fails for TRUTH_CHAIN (requires transitive chain) and OBJECT_TRACKING (requires future-swap awareness). Key bug: BPE splits multi-syllable words — use token-ID subsequence search, not first-word string matching. Script: `/tmp/entity_probe_sweep.py`.
- **Negative results** (2026-04-10): (1) **Probe prefill hurts** — injecting correct ordering as text before Options: 32% vs 37% baseline; SmolLM2 cannot use injected ordering statements. (2) **Metacognitive probe: no signal** — last-token hidden state AUC ≈ 0.50 at all 25 layers; model cannot predict its own CoT success.
- **Route classification probe** (2026-04-10): Last-token hidden state at **L1** classifies 5 route types with 100% 5-fold CV and 92-100% LOO for multi-task families (COMP×3: 92-100%, TRACK×3: 100%). Signal at L1 = structural/syntactic, not semantic. Single-task classes (TRUTH, NAV) need ≥1 training example (LOO zeroes are a data-coverage artifact, not a representation failure). **Replaces keyword routing** — vocabulary-independent, no code changes needed for new phrasing. Script: `/tmp/route_probe_sweep.py`. See memory `route_classification_probe.md`.
- **Vocabulary routing** (2026-04-09, updated 2026-04-10): 18/18 BBH tasks correctly routed. logical_deduction per-example confidence: 76-81% → 97-100% after adding tournament-finish vocabulary. Superseded by route classification probe for production; kept as fast offline fallback. See `experiments/vocab_routing.py`.
- **Self-describing solvers + auto-fit routing** (2026-04-12): `RoutingTurnstyle.build(solvers, model, tokenizer)` trains L1 last-token route probe from each solver's `examples` attribute — 30 real BBH inputs per class. **11/11 routing accuracy** on held-out BBH (index 31, outside training window). Three integration bugs found during wiring: (1) `_navigate_solve()` always returned "Yes"/"No" — added step/turn pattern guard so it returns `None` on non-navigate prompts; (2) `parse_arithmetic()` matched date strings like `7/9/1972` — added early-reject for `\d{1,2}/\d{1,2}/\d{4}`; (3) `SQLTurnstyle.examples` used synthetic pipe-format while all actual BBH penguins are CSV — replaced with `load_task("penguins_in_a_table")`. Rule: solver `examples` must be drawn from `load_task()`, not invented, because format drift is invisible until routing fails.
- **Parser fast-path reliability rule**: any `parse()` that cannot return `None` is a bug — it becomes a catch-all that preempts probe routing for every task type. All parsers must fail gracefully on non-matching input.
- **Untested**: LLM fallback paths never fire on BBH (regex handles 100%). SmolLM2 JSON extraction quality unknown — needs GPU validation.
- **Bonsai 8B**: 1-bit 8B model smoke-tested (194.8 tok/s). Not integrated. MLX path blocked on Python 3.14.
- **BitNet-b1.58-2B-4T**: partner=87.8%@L15, owner=100%@L17 — comparable to SmolLM2-1.7B. Requires bfloat16 (float16 overflows to NaN). Not integrated; staying with SmolLM2.

## Known Issues

- `transformers` 5.4.0 (from Bonsai exploration) breaks `sentence-transformers` (wants <5.0.0). Pin back if embeddings needed.
- **MPS state carry-over from abrupt shutdowns** (2026-04-25): on this hardware, force-killing a `swollm evaluate` process leaves MPS GPU state behind that can cause the *next* long eval to hang silently mid-run (we observed this at hyperbaton's bypass phase). Symptoms: process exits cleanly with `resource_tracker: leaked semaphore` warning and no `bypass:` line; no Python traceback. Bisecting through task subsets does NOT reproduce — bisect runs work because they don't inherit the bad GPU state. Restart the shell or wait long enough for state to clear; the hang resolves on its own. Defensive cli.py change: `torch.mps.empty_cache()` between tasks; `faulthandler.enable()` in main.
